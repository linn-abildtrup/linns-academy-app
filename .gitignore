node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Firebase service account keys (NEVER commit)
scripts/*.json
scripts/service-account-key.json

# Lokale arbejdsfiler (uploades ikke til repo)
/reference
/videos-to-upload
/sounds

# Data-filer til seeding (kan downloades igen, behøver ikke commit)
scripts/*.xlsx
scripts/*.csv

# Claude Code lokale settings
/.claude

# Screenshots til hurtig deling med Claude
/screenshots

# Lokale binaries (hentet fra nettet, ikke commit-vaerdige)
scripts/bin
